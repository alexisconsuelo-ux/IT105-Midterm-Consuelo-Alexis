WHAT CHANGED?
Before, the query slowly scans the table. Then after indexing, it became faster scanning the data.

WHY FASTER?
It scans faster becuase fewer data is being scanned.

WHY INDEXING IS IMPORTANT?
Indexing is important because it effectively and effciently retrieves informatoon faster and is very useful in large datasets.